// PostCSS processa o CSS antes de enviar para o browser
// Tailwind e Autoprefixer são plugins que rodam nesse processo
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
